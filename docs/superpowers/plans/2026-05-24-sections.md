# Implementation Sections — Cupa Mondiala 2026

Splits [2026-05-23-cupa-mondiala-implementation.md](./2026-05-23-cupa-mondiala-implementation.md) into six chat sessions, each producing a coherent, verifiable deliverable. New sessions should read this file plus the plan file before starting.

## Environment notes (apply to every section)

- Existing repo: Next.js 16.2.6 + React 19.2.6 + Tailwind v4 + TypeScript 6.0.3 + Node 20 (pinned via `.node-version`). Tailwind v4 uses `@import "tailwindcss"` and the `@theme` block — the plan's v3 `@tailwind base; ...` syntax does not work.
- AGENTS.md mandate: consult `node_modules/next/dist/docs/` before writing Next-specific code (this is not Next 15).
- **Prisma 7** with the legacy `prisma-client-js` generator. The datasource `url` lives in `prisma.config.ts`, not `schema.prisma`.
- No local Postgres: use `npx prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script --output …` to emit migration SQL without a live DB; commit it under `prisma/migrations/<n>_<name>/`. The `0_init` migration is already committed AND already applied to the Railway Postgres.
- **Build vs start split:** `package.json` build is `prisma generate && next build` (no DB needed at compile time). Start is `prisma migrate deploy && next start -p $PORT` so migrations apply at container boot when Railway has injected env vars. Do NOT put `prisma migrate deploy` back in build — it was there originally and broke the Railway deploy because `DATABASE_URL` isn't available during build.
- **Railway deploy is live** at https://cupamondiala.andrei42.com. Two services in the `appealing-creativity` project: `main-node` (Next.js) and `Postgres`. `main-node` has a `DATABASE_URL=${{Postgres.DATABASE_URL}}` reference variable; that pattern is required for any future env var that needs a cross-service value. Healthcheck is on `/`, so `/` must always return 2xx — keep that in mind if you swap pages.
- **Jest config file is `jest.config.cjs`** (not `.js`). The eslint config has a `*.cjs` override that allows `require()` since next/jest is CJS-only.
- The original InRing teaser at `app/page.tsx` is gone. Currently `app/page.tsx` is a Section-1 placeholder that renders "Cupa Mondiala 2026 — în pregătire" purely to keep the Railway healthcheck green.
- Work on the `master` branch unless told otherwise. Direct push to master is allowed (`.claude/settings.json`).
- Git committer identity is `Andrei Cautisanu <andrei.cautisanu@gmail.com>` — configured locally for this repo only.

## Section 1 — Foundation & Pure Logic (Tasks 1–5) ✅ DONE

Shipped to production on 2026-05-24, commits `0ac71e7..7fc6ffc` on `master`.

**What landed:**
- Scaffold adapted in place (no `create-next-app` re-run): runtime + dev deps installed, `package.json` scripts rewritten, `app/layout.tsx` / `app/page.tsx` / `app/globals.css` replaced with Tailwind v4 syntax.
- Full Prisma schema with `Team.fifaCode @unique` upfront (Section 2 skips the plan's second migration step). `lib/prisma.ts` singleton. Init migration SQL generated via `migrate diff` and committed under `prisma/migrations/0_init/`.
- Jest configured with `next/jest` (file is `jest.config.cjs`). 19/19 tests green: 14 scoring + 5 locking.
- Scoring engine and locking helpers built TDD-first. **Scoring note:** the knockout +2 bonus rule was tightened from the plan — see [[scoring-bonus-rule]] memory.
- Audit patches: TS bumped to 6.0.3, react to 19.2.6, dotenv declared explicitly, tsconfig pre-includes `types/**/*.d.ts` for Section 2's NextAuth augmentation.
- Railway live: Postgres service provisioned, `DATABASE_URL` reference variable wired, `0_init` migration applied to prod DB at container boot.

**Open hand-off for Section 2:** see "First step" in the Section 2 block below.

## Section 2 — Seed + Auth + Shell (Tasks 6–10) ✅ DONE

Shipped to production on 2026-05-25, commits `6450a1a..b4d3efe` on `master`.

**What landed:**
- Seed: 12 groups, 48 teams, 72 group-stage matches, 32 knockout-slot placeholders, invite code `cupa2026`. `prisma/data/teams.ts` + `prisma/data/fixtures.ts` + `prisma/seed.ts`. Seed ran against Railway prod DB.
- **Prisma 7 adapter**: `@prisma/adapter-pg` required (Prisma 7 needs a driver adapter in its new engine). `lib/prisma.ts` rewritten as a lazy Proxy so the build doesn't need `DATABASE_URL`. Future code using `prisma` works unchanged.
- NextAuth v5 beta.31 with Credentials provider + JWT strategy (30 days). `auth.config.ts` (edge-safe, `trustHost: true`) + `auth.ts` + `app/api/auth/[...nextauth]/route.ts` + `types/next-auth.d.ts` augmenting Session/JWT with `id: number` and `isAdmin: boolean`.
- Registration API (`app/api/register/route.ts`) with zod validation, invite-code check, bcrypt. Login + register pages under `app/(auth)/`. `app/providers.tsx` SessionProvider wrapping root layout.
- Middleware (`middleware.ts`): unauthenticated → `/login`; authenticated on auth pages → `/clasament`; admin gate on `/admin/*` and `/api/admin/*`; cron bypass for `/api/admin/sync-results`.
- Bottom-nav shell: `components/BottomNav.tsx`, `app/(app)/layout.tsx` (header + admin link), stub pages for `/clasament`, `/pronosticuri`, `/meciuri`, `/profil`. Sign-out via separate `app/(app)/profil/actions.ts`.
- `app/page.tsx` replaced with auth-aware redirect (`/clasament` if session, `/login` if not).
- **Healthcheck moved**: `/api/health` returns 200; `railway.json` updated to `healthcheckPath: "/api/health"` (old `/` now redirects).
- **`AUTH_SECRET` set** in Railway env via `railway variable set` — was missing, causing `MissingSecret` errors that let all middleware pass-through.

**Prod smoke test (all green):**
- `/api/health` → 200
- `/clasament` unauth → 307 → `/login`
- `/login` unauth → 200
- `/` unauth → 307 → `/login`

**Open hand-off for Section 3:** stub pages are in place. Section 3 replaces `app/(app)/pronosticuri/page.tsx` with the matchday-tabbed predictions UI.

## Section 3 — Match Predictions (Task 11) ✅ DONE

Shipped to production on 2026-05-25, commit `e8f47d6` on `master`.

**What landed:**
- `POST /api/predictions/match` (`app/api/predictions/match/route.ts`) — zod-validated, lock-time enforced via `lib/locking.ts`, upserts on `(userId, matchId)`. Uses the pure helper `buildMatchPredictionUpsertData` from `lib/predictions.ts` to null out `predictsEt`/`predictsPens` for group matches and default them to `false` for KO when omitted.
- `lib/predictions.ts` — pure helpers (`isKnockoutRound`, `buildMatchPredictionUpsertData`) covered by 5 tests.
- `components/CountdownLock.tsx` — client component, ticks every minute; locked → rose pill, unlocked → amber when <1h else slate.
- `components/MatchCard.tsx` — handles placeholder ("Echipe necunoscute"), group, knockout (with ET/Pens toggles), locked (disabled inputs, no save), and shows `pointsAwarded` once present. 7 RTL tests across all branches.
- `app/(app)/pronosticuri/page.tsx` — replaced stub with matchday-tabbed page (GROUP_1 → FINAL, including R32 and THIRD_PLACE), small "X/Y salvate" counter, scroll-snap tab strip, links to clasament-grupe + bonus stubs for Section 4.

**Aesthetic refresh shipped with this section** (was natural to bundle):
- `next/font/google` → **Anton** (display, condensed sport-poster) + **Manrope** (body, geometric warm sans). Exposed via `--font-display` / `--font-sans` CSS variables in `app/layout.tsx`.
- `app/globals.css` — fonts wired into `@theme`, added a fixed radial gradient mesh on `body` for subtle atmosphere; new `.font-display` utility class.
- `app/(app)/layout.tsx` — sticky header with `CupaMondiala` wordmark in the display font, admin badge styled as a pill.
- `components/BottomNav.tsx` — replaced emoji icons with inline SVG glyphs, active state uses a top accent bar + emerald-300 text.

**Test count:** 33 green (was 19 after Section 1, +14 in Section 3: 5 predictions + 2 CountdownLock + 7 MatchCard).

**Prod smoke test (all green):**
- `/api/health` → 200
- `/pronosticuri` unauth → 307 → `/login`
- `POST /api/predictions/match` unauth → 307 → `/login` (middleware intercepts before route)

**Local agent tooling** (`.claude/`, `.superpowers/`) added to `.gitignore`.

**Open hand-off for Section 4:** the `/pronosticuri` page already links to `/pronosticuri/clasament-grupe` and `/pronosticuri/bonus` — Section 4 builds those pages and their POST endpoints.

## Section 4 — Standings + Bonus Predictions (Tasks 12–13)

**Goal:** the remaining two prediction types — group standings (1–4 per group) and bonus (champion, runner-up, top scorer, dark horse).

- `POST /api/predictions/standings` with transactional upsert + uniqueness validation.
- `GroupStandingsForm` component + `/pronosticuri/clasament-grupe` page.
- `POST /api/predictions/bonus` with champion ≠ runner-up + dark-horse pot 3/4 enforcement.
- `BonusForm` component + `/pronosticuri/bonus` page.
- Both forms respect `tournamentLockTime` from Section 1.

**Deliverable:** all four prediction types working; tournament lock blocks edits once the first kickoff has passed.

## Section 5 — Scoring Orchestrator + Admin (Tasks 14–17)

**Goal:** results entry (manual + auto-sync) and the chained recalculation that turns results into points.

- `lib/recalc.ts`: `computePointsForPrediction`, `recalcPointsForMatch`, `recalcGroupStandings`, `recalcDarkHorse`, `recalcChampionAndRunnerUp`, and the `calculateAndStorePoints` master orchestrator. Tests per Task 14 Step 1.
- Admin layout (admin-only) at `/admin/*`.
- Manual results entry: `/admin/rezultate` + `POST /api/admin/results` that calls `calculateAndStorePoints` (Task 15).
- football-data.org client + `POST /api/admin/sync-results` (cron-secret authenticated) that maps fixtures by `externalId`/`fifaCode` and recomputes (Task 16).
- User management + invite-code rotation pages and APIs (Task 17).
- Document the "encode pen winner in the 90-min score" convention surfaced in the plan's self-review.

**Deliverable:** admin can enter a result, points recalc immediately, the sync endpoint exists and authenticates correctly. Tests for `computePointsForPrediction` green.

## Section 6 — Public Views + Deploy (Tasks 18–20)

**Goal:** leaderboard, matches view, production deploy.

- `lib/leaderboard.ts` with category breakdowns + tests for sort ordering.
- Replace `/clasament` stub with the leaderboard table (highlighting current user).
- Replace `/meciuri` stub with the round-grouped fixture/results view.
- Update existing `railway.json` (currently NIXPACKS) per plan Task 20 Step 2, write `.env.example`, push, run seed in prod, promote first admin, configure Railway cron for `*/15` syncing during tournament dates only.
- Production smoke test against the deployed URL.

**Deliverable:** live app at the production URL with at least one admin account, a working leaderboard, and a scheduled (or scheduleable) sync.
